# CLAUDE.md — phoenix-wing

本文件仅作为 Claude 工具入口，不复制仓库开发规范。

- 共享开发规则与命名例外：[`AGENTS.md`](AGENTS.md)
- 当前文档导航：[`doc/文档索引.md`](doc/文档索引.md)
- 当前路线：[`doc/plan.md`](doc/plan.md)

修改公共 API 前必须遵守 `Pnw/pnw/PNW_` 前缀；`kt-codegen` 领域例外以 `AGENTS.md` 为准。TypeScript 使用 NodeNext 时，相对 import 必须带 `.js` 后缀；Rust 构建使用 `--locked`。
