# 0.7.6 发布候选

状态：本地候选，未发布。版本于 2026-09-21 确认。

## 发布范围

仅根包 `phoenix-wing@0.7.6`。本轮为 Vue/UI 与示例改动，所有独立子包、内部依赖精确版本及协议版本保持不变。

- Header 增加 `#center`、`#right`，保留旧插槽；框架浮出/收回独立位于最右侧。
- 完整 View 页面布局分离固定页头与正文滚动，默认 flow 行为保持兼容。
- 工作台显示设置、工具停靠及页头交互示例完善。
- 补充库内 UI 选型与验收标准，去除私有项目描述和示例中的本机路径。

## 点检

- [X] 升版前相关组件与示例回归：8 个测试文件、35 项通过。
- [X] 升版前库构建及示例类型检查通过。
- [X] 0.7.6 版本矩阵、公共版本常量与文档门禁通过。
- [X] 重新构建并打包 0.7.6；公共 dist 的 92 项必需文件及单实例导入图验证通过。此前 0.7.5 本地候选制品不作为本版发布证据。
- [X] 0.7.6 根包全量测试：61 个文件、401 项通过（含标题宽度 token 兼容整改后的复测）。
- [X] 最终 tarball 内容、依赖、隐私与 sourcemap 点检及隔离安装验证。
- [X] 本仓示例及最终 tarball 浏览器验收；精确范围见本页「最终发布前复验」。
- [X] 全量发布门禁、Registry 版本可用性核对与明确发布授权（2026-09-23）。

既有 0.7.5 发布回执和 Registry 示例命令保留其历史含义，不批量替换版本字符串。

## 2026-09-23 发布前修正

用户已授权 pnpm 正式发布及修复发布前发现的问题。原 clean 候选 `c16fd07065c0372dc60ac858969fedcb4f734ba9`
完整 `verify:ci` 通过（根包 61 files / 402 tests），但实际 tarball 的 380px Header 新旧插槽混用时，
center 搜索框仅剩 8px，因此暂停发布，转 working 修复；旧通过记录不代替修复后的重新验证。

- 中区直接子控件不再收缩，超出内容只在中区滚动；right 与框架动作保持独立。
- `PnwSidebarBlock variant="card"` 恢复实线圆角外框与默认正文 inset；默认 strip 和 Primary 横线保持。
- Block Header 操作插槽移除竖线，使用公开工作台 token；示例增加「Block 样式」切换入口。
- 嵌套 strip 标题默认透明、上下横线，保留默认折叠；card 选择器只作用于直接子 Header/正文，不向嵌套 Block 泄漏背景与 padding。
- Main 的「图片与属性」作为一级 Block，右侧小块只传 title/内容；Primary 左右贴边。`--pnw-block-header-bg` 保留消费者自定义入口。
- 修复后测试、实际制品与浏览器回归已完成，见下文；本节不等于 Registry 发布回执。

## 隔离示例验证

- [X] 支持本地 tarball 来源识别，复用本仓示例；错误依赖和版本拒绝回归通过，未放宽 Registry 精确版本检查。
- [X] 隔离安装、类型检查及生产构建通过；页面明确标识未发布的本地制品，不冒充 Registry。
- [X] 最终候选重新打包后检查内容、依赖、隐私与 sourcemap，并完成浏览器交互验收。源码变更后的旧制品结果不自动作为最终验收证据。

## Header 浏览器整改

实际隔离示例发现长标题在 380px 容器中挤没 center 搜索区。修复为标题与中间区共享剩余宽度，无中间区的规则保持不变。保留标题最小宽度 token，默认值调整为零以允许窄屏收缩。

- [X] 修复版重新构建、打包、隔离安装、类型检查与示例生产构建通过；Header/Layout 定向回归 12 项通过。
- [X] 新制品浏览器复测：长标题与 380px 容器下，搜索框可见并可输入，右侧业务按钮与框架按钮可见。
- [X] 首轮制品浏览器验证浮出、页头按钮收回后，搜索文本与保存计数保留。
- [X] 修复版宽屏、旧插槽组合、浮出尺寸调整与回嵌复测，以及 Footer、Primary 停靠验收；未覆盖场景继续明确列于下文。

## 工作台显示偏好浏览器点检

基于上述布局修复版隔离制品，在完整工作台示例执行：

- [X] 从显示设置关闭 Footer 与 Bottom，页面对应区域消失。
- [X] 刷新后由欢迎页重新进入工作台，Footer 与 Bottom 保持关闭。
- [X] Footer 关闭时仍可打开显示设置，恢复 Bottom 并关闭 Primary；正文中的 Primary 展开入口仍存在。
- [X] 当前页面未提供 Secondary，其开关保持禁用，不伪造可用面板。
- [X] 最终制品在提供 Secondary 的页面验证显示与隐藏，Footer 隐藏偏好刷新恢复；资源工具停靠见下文。

本轮仅证明显示偏好恢复。示例刷新后先显示欢迎页，不能据此声称活动 View 自动恢复已经通过。

## 工具停靠浏览器点检

当前 0.7.6 源码重新构建及公共 dist 完整性检查通过。以下在本仓开发示例完成，不代替最终 tarball 验收：

- [X] Primary 收起时打开资源工具，选择资源并输入筛选词，停靠后 Primary 自动展开。
- [X] 浮出与停靠往返保留筛选词及选中资源，不重新初始化工具数据。
- [X] 停靠块可折叠；移动到首部后动作切换为移到尾部，再浮出仍保留状态。
- [X] 标题栏拖动与右下角手柄缩放实际改变窗口位置、尺寸，正文与框架动作仍正常显示。
- [X] 缩放手柄改为位于边框内，避免负偏移被裁剪。浏览器核对八方向手柄中心均命中，右下角中心实际拖动使窗口由 640×480 变为 730×525；浮窗与停靠定向回归 14 项、构建通过。此项为开发示例验证，最终打包复验仍按上文待办。
- [ ] 多实例状态隔离、关闭重开与跨刷新尺寸恢复仍需独立点检，不由单实例往返结果推定。

## 最终发布前复验（2026-09-23）

本轮聚焦 Header 插槽、Block 默认视觉及示例，不扩展多实例/持久化契约。上述工具多实例与跨刷新尺寸恢复保留为后续专项浏览器点检，不声称已验收；现有状态机自动测试仍属于完整门禁。

| 门禁 | 结果 |
| --- | --- |
| `PNW_VERIFY_OFFLINE=1 pnpm verify:ci` | 完整通过；135 个 JS/Vue 测试文件、851 项测试（根包 62/408），类型、Rust、独立包矩阵及所有 tarball 门禁通过 |
| `pnpm example:workbench:typecheck` | 通过 |
| 文档 / 架构 / 聚合 dist | 103 份文档、245 个源码文件、92 项公共必要文件与单实例导入图通过 |
| 精确发布集合 | 仅 `phoenix-wing@0.7.6`；11 个 scoped 包不重复发布 |
| 实际 `pnpm pack` | 381 个文件；内部依赖精确 `code-core@0.6.6`、`db-node@0.6.3`，无本地协议 |
| 隐私 | Git 跟踪文件及实际 tarball（含 sourcemap）未命中所审查的私有消费者、个人主目录、Token/私钥模式；新增 diff 人工复核通过 |
| 隔离安装 | `pnpm example:registry --wing-version 0.7.6 --wing-tarball <候选.tgz> --build-only` 类型与生产构建通过；明确标识 tarball，不冒充 Registry |
| Registry 发布前 | 精确 0.7.6 为 E404，latest 为 0.7.5；已取得用户 pnpm 正式发布授权 |

候选 tarball SHA-256：`f9bc1e87505d66f5f236b5ff33c8ebcd233d534e09240c5dd5b4929979922eca`。

开发页面与隔离 tarball 的真实 Chrome 点检：

- Header 宽/380px 窄容器、新旧插槽组合、无 center、输入与保存、实际手柄缩放和收回保留状态通过。
- Block 在 1280/480/380px × light/dark 下，Main 1px 实线/8px 圆角，Header 无竖线，Primary 左右 gap=0，内容横溢=0。
- 图片 Block 为一级同级；两个内部 strip Header 默认透明、上下 1px 横线，独立折叠 ARIA 正确；窄屏上下排列。
- variant 切换、折叠与 Header 刷新保留草稿；公开标题背景 token 覆盖有效。示例自检 4/4 通过。
- 最终 tarball 的 Secondary 开关、Footer 隐藏后刷新恢复、无 Footer 时设置入口、资源工具停靠自动展开 Primary、首/尾位置切换、再浮出保留筛选与选中资源通过。
- 最终通过轮次浏览器 console warning/error 与未捕获异常均为 0。面板退出动画完成后再核验 DOM，按工具身份区分显示设置与资源工具。

正式 Registry 安装及发布摘要须在发布成功后单独补记；以上不等于已发布。
