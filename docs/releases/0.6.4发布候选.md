# Phoenix Wing 0.6.4 发布候选

状态：current（已发布，消费者回归继续）

Owner：Phoenix Wing maintainers

适用版本：0.6.4

最后核验：2026-08-11

## 范围

0.6.4 是向后兼容的 UI patch。`PnwPrimarySection` 的折叠箭头默认位于标题左侧，
遵循 VS Code 习惯：收起时向右、展开时向下。点击区域、键盘操作、`aria-expanded`、
受控与非受控状态以及 120ms 动画保持不变；消费者不应再写位置特例。

## 独立版本身份

本轮实际发布集合只有根 `phoenix-wing@0.6.4`。`PNW_VERSION` 与根包一致为 0.6.4；
11 个 scoped packages、Rust source 身份及其实现均保持已发布的 0.6.3。根包内部
依赖继续精确使用 `code-core@0.6.3` 与 `db-node@0.6.3`。该边界遵循
[《独立版本发布规则》](../独立版本发布规则.md)，不会为未变化 Core 产生空 patch。

## 发布前门禁

- [x] 文档、架构边界、聚合 dist 与逐包 release matrix 检查通过。
- [x] 根包和 workspace 测试、类型检查、生产构建通过。
- [x] 根聚合包 tarball、内部 0.6.3 精确依赖和完整工作区回归通过；Rust source 只做
  不变性回归，不进入发布集合。
- [ ] Admin 真实消费者验证 Primary Section 左侧箭头、明暗主题和宽窄屏。
- [x] 发布后在无相邻源码的干净 Registry consumer 中核对 `PNW_VERSION === "0.6.4"`
  且根依赖仍解析为 0.6.3。

Admin 真实页面回归属于发布后的消费者验证，不改变本次根 UI 包已经完成的 Registry
发布判定。Git tag 与 push 仍由开发者手工执行。

## Registry 发布回执

2026-08-11，开发者明确授权后在仓库根目录使用 pnpm 10.15.1 发布：

- `phoenix-wing@0.6.4` 已发布，Registry `latest` 为 0.6.4；
- 发布 tarball integrity 为
  `sha512-XonR77TFG0bbkE6Ke2yk/ur6R8RDWOpL/moXbqFLsEj8+Hg0PWh1o+p3IAc8bn3djsY4ML4y/0d/FlO/Iw7M5w==`；
- 11 个 `@phoenix-wing/*` 包没有重复发布，Registry `latest` 均保持 0.6.3；
- 发布 manifest 的 `@phoenix-wing/code-core` 与 `@phoenix-wing/db-node` 均为精确
  0.6.3，不含 `workspace:`、`file:` 或 `link:`；
- 无相邻 Wing 源码的 Registry consumer 安装成功，运行时确认
  `PNW_VERSION === "0.6.4"` 且 `PnwPrimarySection` 可导入；
- 本次没有创建 Git tag，也没有执行 Git push。

## Wing 候选验证

2026-08-10 本地验证确认：

- 独立版本矩阵共 12 个发布单元，根包为 0.6.4，11 个 scoped packages 为 0.6.3；
- 聚合 tarball 的 `code-core` / `db-node` 依赖被转换为精确 0.6.3，无
  `workspace:` / `file:` / `link:`；
- 无相邻源码的隔离 npm consumer 安装、入口导入、SQLite smoke、Vue 单例与
  Vite 生产构建通过；
- `cad-rust-source@0.6.3` tarball 的 Cargo locked release tests 与 query contract 通过。
