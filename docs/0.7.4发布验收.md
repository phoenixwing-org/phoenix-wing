# Phoenix Wing 0.7.4 发布验收

状态：current（已公开发布）

Owner：Phoenix Wing maintainers

适用版本：根包 0.7.4；`code-core` / `kt-codegen` 0.6.6；其他 scoped packages 保持既有版本

最后核验：2026-09-10

## 1. 发布身份

本轮 npm 制品来自同一已验证源码树：

- commit：`d2f73a48409c195a500787c16510c7d05aab1ea0`；
- tree：`9d2c829d51e5f7d6c8687e8cff03aabbefc0988f`；
- parent：`719b6b78fa0bf8803a18912c862779d84e605c13`；
- author / committer time：`2026-09-10T22:43:24+08:00`；
- `PNW_VERSION=0.7.4`；
- `KT_CODEGEN_GENERATOR_VERSION=1.0.2`。

发布完成后源码工作树保持 clean。npm Registry 发布与 Git 历史同步分开管理：本轮没有
执行 Git push，也没有创建或推送 `v0.7.4` tag；发布结果不依赖未存在的 Git tag。

## 2. 实际发布集合

按独立版本规则，本轮只发布三个确有变化或精确内部依赖变化的单元：

| 发布单元 | 版本 | 主要变化 |
| --- | --- | --- |
| `@phoenix-wing/code-core` | `0.6.6` | 完善 `PnwCombo` 焦点、200 项边界与原生下拉主题配对 |
| `@phoenix-wing/kt-codegen` | `0.6.6` | 参数表/预检 Block Header、构造函数结束标记与 Combo 回填注释；生成规则固定为 `1.0.2` |
| `phoenix-wing` | `0.7.4` | 汇总本轮 UI 能力并把 `code-core` 精确内部依赖发布为 `0.6.6` |

其余九个 scoped packages 没有本轮源码或发布依赖变化，未空升 patch、未重复 publish。
根包发布 manifest 对内部包使用精确版本：`@phoenix-wing/code-core@0.6.6` 与
`@phoenix-wing/db-node@0.6.3`，不含 `workspace:`、`file:` 或 `link:`。

发布顺序为：

1. `@phoenix-wing/code-core@0.6.6`；
2. `@phoenix-wing/kt-codegen@0.6.6`；
3. `phoenix-wing@0.7.4`。

三个目录均只执行 `pnpm publish --access public --no-git-checks`。未使用 `npm publish`、
`pnpm --dir ... publish`、递归 publish 或消费者依赖 override。

## 3. 发布前完整门禁

在 clean `develop@d2f73a4` 上完成：

- [x] `pnpm install --frozen-lockfile`；
- [x] `pnpm -r --if-present build`；
- [x] `pnpm test:workspace`：TypeScript / Vue 共 128 files / 810 tests；Rust 工作区测试全过；
- [x] `pnpm -r --include-workspace-root --if-present typecheck`；
- [x] `PNW_VERIFY_OFFLINE=1 pnpm verify:ci`；
- [x] 文档 97 篇、架构 241 source files / 9 pure graphs / 3 view roots；
- [x] 12 包 release matrix、根聚合产物 87 个必需文件及 singleton graph；
- [x] TypeScript tarball 隔离 consumer：1 个聚合包与 10 个 scoped 包通过；
- [x] SQLite smoke、聚合 UI singleton、Vite consumer 与 Rust source tarball 隔离重建；
- [x] tarball/manifest 不含本地路径依赖，版本和公开入口与 release matrix 一致。

以上门禁属于发布节点本身。Windows 原生下拉、CAA 与实际 clang-format 的补充实机验收
继续作为消费者/平台回归，不反向否定已完成的跨平台纯逻辑、制品和 Registry 发布门禁。

## 4. Registry 正式回执

2026-09-10 发布后重新查询 exact 版本和 dist-tag，结果如下：

| 发布单元 | Registry shasum | Registry integrity | `latest` |
| --- | --- | --- | --- |
| `@phoenix-wing/code-core@0.6.6` | `9f7d221574545cb2bc959d9092dd07f13e41ac52` | `sha512-gW4m9nbD67j+mK+e6LYpf/GmCWLsJ812a6MmFtwS+M0Yg8rIdd2hFyWDhGdlNvAobT/prXoLy+7w+sm9uLVauA==` | `0.6.6` |
| `@phoenix-wing/kt-codegen@0.6.6` | `320fc71f034a6c876f7b32b1c5c8c38ccce3ef6a` | `sha512-i3b4edJtoVbaV6arUwghj92TnntDWl9ehupHiYGgURsNjUFE8i1A3+Y/nFpn3USTwYqYPUjpDryU0OEDuNgZAg==` | `0.6.6` |
| `phoenix-wing@0.7.4` | `9e3c533f06a916427d754663e90c633f02d63553` | `sha512-RnNzYwnUCQjC1e6HoyBYmaeoVCF2KlyeJ/XZKRhKeqT/yT2w5r4rzk3KwF0b3pRb6diq5NoCxGMLULy7h2V+Cw==` | `0.7.4` |

根包发布后曾短暂处于 Registry 索引处理阶段；`pnpm publish` 已明确成功，因此没有重复
发布同一版本。后续查询已确认 exact 版本存在且 `latest=0.7.4`。

## 5. 发布后干净消费

在不包含相邻 Wing 源码的临时目录中完成 Registry 消费验证：

- 精确安装 `phoenix-wing@0.7.4`、`@phoenix-wing/code-core@0.6.6` 与
  `@phoenix-wing/kt-codegen@0.6.6`，依赖树命中三个 Registry exact 版本；
- 已安装 manifest 不含 `workspace:`、`file:` 或 `link:`；
- `@phoenix-wing/code-core/ui` 可读取 `PnwCombo`、`PnwCleanupDialog`，
  `KT_CODEGEN_GENERATOR_VERSION === "1.0.2"`；
- 根 UI 包通过真实 Vite production build（1768 modules）；
- 临时 consumer 验证结束后已清理，不保留本机路径或临时依赖状态。

根包是带 CSS/Vue 入口的浏览器 UI 包；直接交给无 CSS loader 的裸 Node ESM 不是支持的
根包消费形状。发布制品已经通过带 CSS loader 的 tarball gate 与 Vite Registry consumer，
不把裸 Node 的 CSS 扩展解析限制记为包缺陷。

## 6. 消费者与后续边界

- 发布前已有真实扩展消费者完成标准并列源码构建、测试与交互验证；这些结果证明候选行为，
  不冒充 Registry 安装结果。
- Registry 发布完成后已通知扩展消费者把正式 manifest/lock 精确升级到对应版本，并独立
  执行 frozen install、类型、测试、构建与 Host 回归；其最终回执由消费者仓自行记录。
- Wing 不直接修改消费者 manifest、lockfile、缓存或 `node_modules`，也不使用本地 alias
  伪造 Registry 成功。
- Git push 与 `v0.7.4` tag 继续等待开发者单独决定；npm 0.7.4 发布本身已经完成。

