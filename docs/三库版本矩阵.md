# Phoenix 三库版本矩阵

机器可读真源为仓库根目录的 [`release-matrix.json`](../release-matrix.json)，分别固定
每个 Wing 发布物的精确版本与协议主版本。Wing 单仓 CI 运行
`pnpm verify:release-matrix`。消费者的精确依赖版本、锁文件与验收门禁由各自仓库
维护。0.6.4 起默认采用[按包独立版本](独立版本发布规则.md)。

> 发布状态：0.4.0 是含未转换 `workspace:` 的失败制品；锁步修复版本 0.4.1 已完成八包发布、Registry manifest、npm/pnpm 干净安装和 Open Issue、Auto Code、Desk Tools 真实消费验收，详见[《0.4.0 阻断与 0.4.1 修复验收》](0.4.0发布验收.md)。

> 上一消费基线：Wing 0.4.2 八个发布单元已公开发布；Open Issue `e86fe3d`、Auto Code `febcaf4` 与 Desk Tools `411d27c` 已全部精确升级并通过 Registry fixture、测试和生产构建。发布与消费证据见[《0.4.2 本地发布候选验收》](0.4.2发布候选验收.md)。

> 当前代码正在准备根 **phoenix-wing@0.7.3**；scoped packages 继续按
> `release-matrix.json` 保持各自独立版本，不能把本地候选或 sibling 验证冒充 Registry
> 结果。候选范围见[《0.7.3 发布候选》](0.7.3发布候选.md)。

## 当前独立发布单元

| 发布物 | 源码版本 | 消费方 | 约束 |
| --- | --- | --- | --- |
| `@phoenix-wing/code-core` | 0.6.5 | KT Auto Code、Desk server | 纯 TS；不得带入 Vue/Element/SQLite/Rust |
| `@phoenix-wing/git-core` | 0.6.4 | KT Auto Code | 纯 TS Git 摘要、连续历史与改写计划 |
| `@phoenix-wing/git-node` | 0.6.5 | KT Auto Code | Node 22 Git CLI、仓库读取与隔离 worktree 事务 |
| `@phoenix-wing/run-core` | 0.6.4 | KT Auto Code | 纯 TS Run 项目/目标、CAA/CMake 分类与平台计划 |
| `@phoenix-wing/run-node` | 0.6.4 | KT Auto Code | Node 22 目标发现、Task 导入、launch plan 与受控清理 |
| `@phoenix-wing/kt-codegen` | 0.6.5 | KT Auto Code、Desk Tools | 纯 TS；共享 Parameter、legacy、marker、Analyze/Renderer、Apply 投影及 Host-neutral 表格/Primary 组件；不含文件系统与 Host API |
| `@phoenix-wing/cad-contracts` | 0.6.3 | KT Auto CAD、Desk server | native/provider/query 协议与无驱动只读查询共同真源 |
| `@phoenix-wing/cad-core` | 0.6.3 | KT Auto CAD、Desk server | 纯 CAD 计算 |
| `@phoenix-wing/workspace-schema` | 0.6.3 | KT Auto CAD、Desk server | workspace Schema v13 与固定 DDL 哈希 |
| `@phoenix-wing/db-node` | 0.6.3 | Desk server | Node SQLite adapter；不得进入 VSIX |
| `@phoenix-wing/cad-rust-source` | 0.6.3 | Desk 构建阶段 | Rust 源码；不得进入 VSIX或运行时安装脚本 |
| `phoenix-wing` | 0.7.3 candidate | Admin、Desk web-ui 与其他 Vue Host | Vue/UI 聚合兼容包；Code/CAD 新代码不再依赖 |

Wing 根包正在准备 `0.7.3`，其内部依赖精确指向候选 `code-core@0.6.5` 与既有
`db-node@0.6.3`；本轮发布集合及顺序见候选文档，未变化发布物不产生空 patch。表中的产品名称只说明这些
能力曾被哪些产品消费或验证；Registry 公开状态与消费者当前精确版本仍以实际查询及
各消费仓库自己的 manifest、lockfile 和依赖门禁为准，不回写 Wing。

协议主版本独立于 npm 版本：KtCodegen Plan v1、Ribbon contribution v1、CAD native v1、CAD query v1、native provider v1、workspace Schema v13。协议不兼容时宿主必须阻止调用，不能用 npm 版本猜测兼容；canonical fixture 由 Wing 随包发布，消费仓直接导入 Registry fixture，只验证 adapter 与字节一致性。
