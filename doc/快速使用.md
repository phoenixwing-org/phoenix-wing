# 快速使用

```bash
npm install phoenix-wing
```

## 工具函数

```ts
import {
  pnwFormatDuration,
  pnwScheduleDebounced,
  pnwBindPointerDrag,
  pnwResolveColorScheme,
  pnwClearPhoenixBrowserStorage,
} from 'phoenix-wing'
```

## 确认对话框

```ts
import { pnwPromptChoice } from 'phoenix-wing'

const result = await pnwPromptChoice({
  title: '确认删除',
  message: '此操作不可撤销',
  choices: [
    { id: 'delete', label: '删除', variant: 'danger' },
    { id: 'cancel', label: '取消' },
  ],
})

if (result.choiceId === 'delete') {
  // 执行删除
}
```

## Vue3 控件

```ts
// 模态框
import PnwAppModalOverlay from 'phoenix-wing/components/PnwAppModalOverlay.vue'

// 展开图标
import PnwExpandCaret from 'phoenix-wing/components/PnwExpandCaret.vue'

// 组合输入框
import PnwComboTextInput from 'phoenix-wing/components/PnwComboTextInput.vue'
```

## 壳层布局组件

```ts
// 可折叠侧栏块
import PnwSidebarBlock from 'phoenix-wing/layout/PnwSidebarBlock.vue'
// 页面标题头
import PnwPageHeader from 'phoenix-wing/layout/PnwPageHeader.vue'
// 欢迎页骨架
import PnwWelcomeShell from 'phoenix-wing/layout/PnwWelcomeShell.vue'
// 日志面板
import PnwShellLogPanel from 'phoenix-wing/layout/PnwShellLogPanel.vue'
// 页面标签栏
import PnwWorkbenchTabBar from 'phoenix-wing/layout/PnwWorkbenchTabBar.vue'
```

## Ribbon 工具栏

```ts
import {
  PnwRibbonShell,
  PnwRibbonTabBar,
  PnwRibbonGroup,
  PnwRibbonToolButton,
} from 'phoenix-wing/layout/PnwRibbonShell.vue'
// 注：子组件从各自路径导入
```

## 工作台引擎

```ts
import { pnwCreateWorkbench } from 'phoenix-wing'

const wb = pnwCreateWorkbench({
  pagePolicy: (pageId) => ({ maxTabs: 1, tabEnabled: true }),
  navLabel: (pageId) => pageId,
})

wb.openTab({ pageId: 'my-page', title: '我的页面' })
```

## Composable

```ts
import {
  usePnwDocumentTitle,
  usePnwRibbonTabs,
  usePnwResizableTable,
  usePnwPagePropertySheet,
} from 'phoenix-wing'

import { pnwRegisterRibbonIcons, pnwRegisterUrlParser } from 'phoenix-wing'
```

## 属性表

```ts
import {
  pnwPropGroup,
  pnwPropBool,
  pnwPropEnum,
  pnwPropString,
  pnwPropNumber,
  pnwPropPath,
  pnwPropReadonly,
  pnwPropSheet,
} from 'phoenix-wing'
```

## 异步任务进度

```ts
import {
  pnwCreateScanTaskState,
  pnwUpdateTaskFromPoll,
  pnwUpdateTaskFromStreamEvent,
  usePnwAsyncTaskStore,
} from 'phoenix-wing'
```
