# Phoenix Wing 0.4.3 本地发布候选验收

状态：current

Owner：Phoenix Wing maintainers

适用版本：0.4.3

最后核验：2026-07-18

## 候选范围

0.4.3 继续采用八个 npm 发布单元锁步版本，承载 0.4.2 公开发布后的三项 Wing 侧改进：

- `KtCodegenTable` 提炼纯 ViewModel，列宽、对齐、禁用态和 CSS class 等视觉规则进入可测试的 visual primitive；公共 browser 入口、方法与事件保持兼容。
- Kevin 控制块明确为严格同级、禁止嵌套；任何下一条语法完整 Start 或 End 都会终止当前打开块。下一 Start 使旧块只报一次 `marker.missing-end` 并从新 Start 恢复；错配 End 使旧块报 `marker.missing-end`、该 End 独立报 `marker.orphan-end`，不再携带旧状态向后级联。
- Analyze 可保留恢复后的完整区域和 artifact，但任一 marker error 仍使 `canApply=false`，消除级联误报不降低写入门禁。

其他七个包没有新增协议行为，仍因锁步发布政策同步提升版本。KtCodegen Plan、CAD native/query/provider 与 workspace Schema 主版本均未改变。

## 本地候选门禁

公开发布确认前必须同时成立：

1. 八个 `package.json`、聚合常量、内部 workspace 依赖、Rust source manifest、`fcstd-query` crate 与 `release-matrix.release_version` 均为 0.4.3。
2. `release-matrix.json` 的消费者 required 在发布前继续记录实际 Registry 0.4.2；不得提前声称消费者已经升级。
3. `pnpm verify:ci` 全绿，覆盖文档、AST 依赖方向、构建、聚合单例、版本矩阵、全 workspace 测试/类型检查、TypeScript 与 Rust tarball。
4. 干净 tarball consumer 不包含 `workspace:`、`link:` 或 `file:`，聚合 UI 仍保持 Vue/Pinia 单例。
5. 未推送的 Wing 改动归并为一个中文发布候选提交；工作树干净后创建本地注释标签 `0.4.3`，标签必须解引用到该提交。
6. 发布必须使用 pnpm；直接 `npm publish` 继续由 `prepublishOnly` 拒绝。

Marker 隔离回归采用最小化 `PNXBomAnalysis` 反面夹具：构造与析构两个连续 Start 缺 End，后续五个同级块完整。发布前必须证明结果精确为两条 `marker.missing-end`、五个 region、五个 artifact、`canApply=false`，且两个缺失块之间的手写代码不进入任何 replacement region。当前 `kt-codegen` 已完成 **24 files / 91 tests**、typecheck、build 和本地 tarball 行为复核。

## 发布与消费顺序

获得用户公开发布确认后，固定按以下顺序执行：

1. `@phoenix-wing/code-core`
2. `@phoenix-wing/kt-codegen`
3. `@phoenix-wing/cad-contracts`
4. `@phoenix-wing/cad-core`
5. `@phoenix-wing/workspace-schema`
6. `@phoenix-wing/db-node`
7. `@phoenix-wing/cad-rust-source`
8. `phoenix-wing`

每个 scoped package 发布后必须立即查询 Registry 0.4.3；七个 scoped packages 均可查询后才允许发布聚合包。随后在无相邻 Wing 源码的临时目录完成干净安装与 smoke，再依次升级 Auto Code、Desk Tools 和 Open Issue 的精确 Registry 依赖、锁文件与消费门禁，最后独立提交发布结果和消费者矩阵。

## 当前边界

- npm 公开发布：尚未执行，等待用户确认。
- 本次 Start/End 边界修正发生在既有本地 `0.4.3` 标签之后；该标签当前不得用于发布。公开发布前须在用户确认下重新完成全仓门禁并封签最终 0.4.3 提交。
- Auto Code、Desk Tools、Open Issue：仍消费 Registry 0.4.2。
- Auto Code 升级时必须同时递增 Preflight generator version，拒绝复用旧扫描诊断缓存。
- 本轮不执行 Windows 验证；Windows/VS Code/Tauri 原生行为由用户手工点检，不在候选报告中宣称通过。
