  // START KEVIN CAA WIZARD SECTION KtCourseGuardCmd CMD ACTION PDA

  // clang-format off
  //.............................................................................
  // @key    CmdActionPda
  // @usage  put this code block into the function Cmd::ActionSelectorListPda()
  // @brief  InitializeAcquisition and HSO Append
  //.............................................................................
  // Field count = 4
  if (fieldChange) KT_AUTO_HSO_CLEAR();
  switch (field) {
  case Field_KtCourseGuardCmd_Origin:
      KT_AUTO_CMD_ACTION_PDA(Origin);
      break;
  case Field_KtCourseGuardCmd_Targets:
      KT_AUTO_CMD_ACTION_PDA(Targets);
      break;
  case Field_KtCourseGuardCmd_Multi:
      // Please write your own code outside.
      // Because MultiList is not supported.
      break;
  case Field_KtCourseGuardCmd_View:
      // Please write your own code outside.
      // Because the component count is 0.
      break;
  }

  // clang-format on
  // END KEVIN CAA WIZARD SECTION KtCourseGuardCmd CMD ACTION PDA
